# Projeto para o Hackathon Fiocruz

## Usefull commands:

### Enter in docker compose containers:

```sh
docker-compose exec api bash
```

## On clone:

```sh
git submodule update --init --recursive
```

